%% Testing out some cool parametric curves


close all
clear all

t = [0:0.1:100];

Y = sin(t).*(exp((cos(t))) - 2*cos(4*t) - sin(t/12).^5);
Z = cos(t).*(exp((cos(t))) - 2*cos(4*t) - sin(t/12).^5);

scale_factor = 0.1;
Y = X.*scale_factor;
Z = Y.*scale_factor;

x_offset = 0.35;
y_offset = 

plot(X, Y);
