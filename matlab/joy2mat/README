The purpose of joy2mat is to serve as an interface between the 3dconnection 
mouse and matlab, in particular on OSX, and in general on any operating system
supported by 3dconnection. 

Dependencies:
  matlab
  python2.6 or higher
  pygame

Usage:
  In a console:
  python joy2mat.py

  In matlab:
  j = joystick
  j.pull  # Returns a single joystick event struct
  j.pullall # Returns all of the current joystick events in a struct 

- Uriah Baalke ( uriahjb@gmail.com )
